<!-- graph-memory:bootstrap -->
# Project Context — Keel3/keel3_oliver_demo

_Auto-generated by graph-memory. Custom sections are preserved between regenerations._

## Tech Stack

- Docker droplets with oliver-bridge.mjs for agent provisioning
- openclaw framework with 12K default bootstrapMaxChars cap (20K config override does not reach openclaw.json)
- deploy-migrate.mjs for SQL migrations with line-based statement splitting

## Conventions

- Session-start prompt/tool freezes and gateway plugin-manifest caches can hide hotfixes until fresh session or service restart
- Branch-feature smoke requires push — droplet reads from deployed branch, local-only changes never reach live test surface
- Per-project agents are provisioned lazily on first UI visit; existing agents survive reprovision via snapshot-restore and do NOT pick up generator changes
- deploy-migrate.mjs SQL splitter does not strip comments before checking terminators — semicolons in SQL comments inside CREATE TABLE will split the statement
- DigitalOcean rejects cloud-init user_data >64KB; gzip-compress the payload
- Oliver droplet has two prompt file locations per project: agents/project-X/agent/ (older) and workspace/projects/project-X/ (current)

## Procedures

- When auditing tool descriptions for security contradictions, accept audit-finding anchors as citations alongside corpus entries
- For gws_drive_upload: drop cross-references to gws_exec in descriptions — both stewards rejected exposing it as it punctures a two-layer security boundary
- Always edit oliver-bridge-source.ts:167 in lockstep with registry — it carries a duplicate tool description

## Active Work

- Compaction pipeline targeting 12K per-file caps — AGENTS.md compacted from 18,777 to 11,983 bytes
- gws_drive_upload security fix: option (c) — drop cross-reference, edit lockstep at oliver-bridge-source.ts:167
- Agent audit: Phase B+C prompt edits only apply to NEW provisioning — existing per-project agents retain pre-audit content

## Open Threads

- Existing per-project agents still carry stale pre-audit content — need manual recreation or bridge fix to update them

## Guardrails

Do NOT:

- Known AI writing tells that must be aggressively eliminated in Acellus blog content: em-dash flooding, stacked negations, meta-narration, section proliferation, constructed vignettes, pitch-deck regis
- Every new LLM-powered feature must surface a model picker in the UI, even when the backend already supports model_override. Never hardcode model selection.
- Never add comments to code unless explicitly asked — user has corrected this multiple times across sessions

## User Preferences

- Demo scripts use two-file workflow: .md master (shooting plan with beats) + .txt teleprompter companion (short speakable lines, stage cues in brackets)
- Values data integrity and validated pipelines; validates end-to-end before cleanup

## Decision Patterns

- Prefers vertical-slice delivery: one end-to-end path with full stack depth over comprehensive but shallow
- Front-loads code implementation, skips validation phases to move faster
- Grants broad autonomous execution once scope agreed — terse approvals signal trust to self-manage without check-ins
- Accepts pragmatic 'good enough' over perfectionist extra cycles when remaining gap is cosmetic

## Key Patterns & Decisions

- **architecture/agent-core-v2-dead-code-audit**: Steward audit isolated dead chat UI residue to agent-core v2, not Mastra or OpenClaw; OpenClaw thinking status remains live.
- **architecture/caddy-sse-buffering-pitfall**: Caddy reverse proxy buffers SSE responses by default. Must add flush_interval -1 to proxy config for real-time streaming. Caused invisible SSE delay in Oliver pipeline.
- **architecture/claude-prompt-injection-bot-comment-detection**: Claude refuses to execute when a bot opens a PR with instruction files and comments 'read and execute' — correctly identified as prompt injection. Fix: inline instructions directly in the comment.
- **architecture/domain-steward-system**: Domain stewards expanded from 8→10 (added steward-mastra, steward-openclaw, steward-business-logic; split steward-platform) and from 2→5 operating modes (added Investigate, Impact Assessment, Risk Rev
- **architecture/e2b-custom-template-resource-config**: E2B resource limits set at template build time, not sandbox creation. Template selection is two-dimensional: runtime fit (Node.js vs desktop) AND resource capacity.
- **architecture/e2b-port-binding-requirement**: E2B sandboxes require 0.0.0.0 binding, not localhost. Proxy can't reach localhost-bound services. Next.js fix: -H 0.0.0.0.
- **architecture/e2b-process-crash-vs-timeout**: E2B sandbox failures have two distinct root causes: (1) timeout/inactivity kills the sandbox, (2) the process inside crashes (OOM/CPU). The Closed Port Error looks identical from outside — diagnose vi
- **architecture/e2b-sandbox-inactivity-timeout-and-pause**: E2B timeoutMs is an inactivity timeout (not absolute) — resets on connect; use onTimeout pause to preserve sandbox state on inactivity rather than killing it.
- **architecture/e2b-sandbox-stale-preview**: E2B sandbox stale preview from browser caching. python3 http.server reads disk correctly but sends cache-friendly headers. Fix: kill-restart server on reconnect.
- **architecture/e2b-sandbox-timeout-property**: E2B Sandbox.create() accepts 'timeoutMs', not 'timeout'. Wrong property silently ignored, defaulting to 5min lifetime.
- **architecture/graph-memory/scribe-subagent-permission-gap**: Scribe subagents dispatched as background agents fail when Read tool access is denied for paths outside the project directory — Bash tool must be used instead.
- **architecture/mastra-connector-gap-analysis**: Firecrawl is the only connector wired to Mastra; Granola (4 tools via MCP) and Google Workspace/Gmail/Calendar (8 tools via OAuth) remain OpenClaw-only. Pattern established: loadUserConnectorKey → fac
- **architecture/mastra-tool-label-mapping**: AgentActivityPanel ignored displayName from tool_status events. Fixed to prefer displayName over re-rendered tool name.
- **architecture/nextjs-cancel-never-fires-on-client-disconnect**: Next.js does not call cancel() on a ReadableStream when the client disconnects mid-SSE — the reliable drain trigger is pull()'s catch block when controller.enqueue() throws.
- **architecture/oliver-abort-on-new-message-stream-gap**: New user message aborts previous SSE stream — gap where old polling may still be running; requires abort coordination between messages.

<!-- custom start -->
<!-- Add your custom instructions here. This section is preserved during re-bootstrap. -->
<!-- custom end -->
